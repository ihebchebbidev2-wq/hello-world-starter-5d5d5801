<?php

/**
 * Service providers loaded at boot time.
 *
 * Laravel 11 dropped the legacy `config/app.php` providers array — this
 * file replaces it. Add a provider here when you need it autoloaded.
 */

return [
    App\Providers\AppServiceProvider::class,
];
